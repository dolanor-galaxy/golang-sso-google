<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="/css/main.css">
  </head>
  <body>
    <div class="wrapper">
      {{if .seen}}
          <h1>Welcome back, {{ .name }}!</h1>
      {{else}}
          <h1>You are new, {{ .name }}!</h1>
      {{end}}

      <img src="{{ .picture }}" />

      <p>
        <a href="/ok/internal">Come on in</a>
      </p>
    </div>
  </body>
</html>
